# Server
PORT=4001
NODE_ENV=development

# Open-Meteo uses no API key — these are base URLs you can override for testing
GEOCODING_BASE_URL=https://geocoding-api.open-meteo.com/v1
WEATHER_BASE_URL=https://api.open-meteo.com/v1
